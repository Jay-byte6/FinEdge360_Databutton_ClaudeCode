# FinEdge360 Frontend Environment Variables
# Copy this file to .env and fill in the values

# Backend API URL
# For local development: http://localhost:8001
# For production: Your deployed backend URL (MUST be HTTPS for production)
# Example: https://finedge360-backend.railway.app
VITE_API_URL=http://localhost:8001

# Databutton Configuration (if using Databutton platform)
DATABUTTON_PROJECT_ID=
DATABUTTON_CUSTOM_DOMAIN=
DATABUTTON_EXTENSIONS=

# Supabase Configuration (Optional - if using Supabase directly from frontend)
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-key-here

# Notes:
# - All VITE_* variables are exposed to the frontend bundle
# - Never commit sensitive keys to version control
# - For production deployment on Vercel, set these in the Vercel dashboard
# - VITE_API_URL MUST use HTTPS in production to avoid "Not Secure" warnings
